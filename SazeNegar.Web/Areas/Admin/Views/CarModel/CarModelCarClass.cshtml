@using Kendo.Mvc.UI
@model List<SazeNegar.Web.ViewModels.CarModelCarClassViewModel>
@{
    ViewBag.Title = "کلاس ها";

    List<TreeViewItemModel> treeItems = ViewBag.TreeItems as List<TreeViewItemModel>;
}
<input type="hidden" id="nav_active" value="auth_control">
<input type="hidden" id="nav_item_active" value="roles">
<div class="col-lg-6">
    <div class="card card-custom gutter-b example example-compact">
        <div class="card-header">
            <h3 class="card-title">
                کلاس : @ViewBag.RoleName
            </h3>
        </div>
        @using (Html.BeginForm())
        {

            <div style="padding:20px">
                @Html.Kendo().TreeView().Name("carClasses").BindTo(treeItems).Checkboxes(checkboxes => checkboxes.Name("selectedCarClasses")).HtmlAttributes(new { style = "min-height:100px; ", @class = "checkbox" })
            </div>
            <br />
            <div class="card-footer">
                <input type="hidden" name="CarClassId" id="CarClassId" value="@ViewBag.CarClasssID" />
                <a href="@Url.Action("Index")" class="btn btn-secondary">انصراف</a>
                <input type="submit" value="ذخیره تغییرات" class="btn btn-primary" />
            </div>
        }
    </div>
</div>